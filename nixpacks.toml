[phases.setup]
nixPkgs = ["nodejs_22", "npm-9_x", "curl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'Build completed'"]

[start]
cmd = "npm start"

[variables]
PORT = "8080"

[healthcheck]
interval = "60s"
timeout = "30s"
retries = 5
start_period = "120s" 